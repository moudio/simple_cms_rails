<!DOCTYPE html>
<html lang="en">
<head>
  <title>Simple CMS : <%= @page_title || 'Admin Area' %> </title>
</head>
<body>
  <% if !flash[:notice].blank? %>
    <div class="notice">
      <%= flash[:notice] %>
    </div>
  <% end %>
  <%= yield %>
</body>
</html>
